name: Notificar cambio a n8n

on:
  push:
    paths:
      - 'documentation.md'

jobs:
  notificar:
    runs-on: ubuntu-latest
    steps:
      - name: Llamar Webhook
        run: |
          curl -X POST https://rural-jessica-shorts-aware.trycloudflare.com/webhook/github-update
    
